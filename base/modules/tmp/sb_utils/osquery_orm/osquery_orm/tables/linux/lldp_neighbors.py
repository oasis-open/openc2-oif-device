"""
OSQuery lldp_neighbors ORM
"""
from osquery_orm.orm import BaseModel
from peewee import BigIntegerField, IntegerField, TextField


class LldpNeighbors(BaseModel):
    """
    LLDP neighbors of interfaces.
    """
    interface = TextField(help_text="Interface name")
    rid = IntegerField(help_text="Neighbor chassis index")
    chassis_id_type = TextField(help_text="Neighbor chassis ID type")
    chassis_id = TextField(help_text="Neighbor chassis ID value")
    chassis_sysname = TextField(help_text="CPU brand string, contains vendor and model")
    chassis_sys_description = IntegerField(help_text="Max number of CPU physical cores")
    chassis_bridge_capability_available = IntegerField(help_text="Chassis bridge capability availability")
    chassis_bridge_capability_enabled = IntegerField(help_text="Is chassis bridge capability enabled.")
    chassis_router_capability_available = IntegerField(help_text="Chassis router capability availability")
    chassis_router_capability_enabled = IntegerField(help_text="Chassis router capability enabled")
    chassis_repeater_capability_available = IntegerField(help_text="Chassis repeater capability availability")
    chassis_repeater_capability_enabled = IntegerField(help_text="Chassis repeater capability enabled")
    chassis_wlan_capability_available = IntegerField(help_text="Chassis wlan capability availability")
    chassis_wlan_capability_enabled = IntegerField(help_text="Chassis wlan capability enabled")
    chassis_tel_capability_available = IntegerField(help_text="Chassis telephone capability availability")
    chassis_tel_capability_enabled = IntegerField(help_text="Chassis telephone capability enabled")
    chassis_docsis_capability_available = IntegerField(help_text="Chassis DOCSIS capability availability")
    chassis_docsis_capability_enabled = IntegerField(help_text="Chassis DOCSIS capability enabled")
    chassis_station_capability_available = IntegerField(help_text="Chassis station capability availability")
    chassis_station_capability_enabled = IntegerField(help_text="Chassis station capability enabled")
    chassis_other_capability_available = IntegerField(help_text="Chassis other capability availability")
    chassis_other_capability_enabled = IntegerField(help_text="Chassis other capability enabled")
    chassis_mgmt_ips = TextField(help_text="Comma delimited list of chassis management IPS")
    port_id_type = TextField(help_text="Port ID type")
    port_id = TextField(help_text="Port ID value")
    port_description = TextField(help_text="Port description")
    port_ttl = BigIntegerField(help_text="Age of neighbor port")
    port_mfs = BigIntegerField(help_text="Port max frame size")
    port_aggregation_id = TextField(help_text="Port aggregation ID")
    port_autoneg_supported = IntegerField(help_text="Auto negotiation supported")
    port_autoneg_enabled = IntegerField(help_text="Is auto negotiation enabled")
    port_mau_type = TextField(help_text="MAU type")
    port_autoneg_10baset_hd_enabled = IntegerField(help_text="10Base-T HD auto negotiation enabled")
    port_autoneg_10baset_fd_enabled = IntegerField(help_text="10Base-T FD auto negotiation enabled")
    port_autoneg_100basetx_hd_enabled = IntegerField(help_text="100Base-TX HD auto negotiation enabled")
    port_autoneg_100basetx_fd_enabled = IntegerField(help_text="100Base-TX FD auto negotiation enabled")
    port_autoneg_100baset2_hd_enabled = IntegerField(help_text="100Base-T2 HD auto negotiation enabled")
    port_autoneg_100baset2_fd_enabled = IntegerField(help_text="100Base-T2 FD auto negotiation enabled")
    port_autoneg_100baset4_hd_enabled = IntegerField(help_text="100Base-T4 HD auto negotiation enabled")
    port_autoneg_100baset4_fd_enabled = IntegerField(help_text="100Base-T4 FD auto negotiation enabled")
    port_autoneg_1000basex_hd_enabled = IntegerField(help_text="1000Base-X HD auto negotiation enabled")
    port_autoneg_1000basex_fd_enabled = IntegerField(help_text="1000Base-X FD auto negotiation enabled")
    port_autoneg_1000baset_hd_enabled = IntegerField(help_text="1000Base-T HD auto negotiation enabled")
    port_autoneg_1000baset_fd_enabled = IntegerField(help_text="1000Base-T FD auto negotiation enabled")
    power_device_type = TextField(help_text="Dot3 power device type")
    power_mdi_supported = IntegerField(help_text="MDI power supported")
    power_mdi_enabled = IntegerField(help_text="Is MDI power enabled")
    power_paircontrol_enabled = IntegerField(help_text="Is power pair control enabled")
    power_pairs = TextField(help_text="Dot3 power pairs")
    power_class = TextField(help_text="Power class")
    power_8023at_enabled = IntegerField(help_text="Is 802.3at enabled")
    power_8023at_power_type = TextField(help_text="802.3at power type")
    power_8023at_power_source = TextField(help_text="802.3at power source")
    power_8023at_power_priority = TextField(help_text="802.3at power priority")
    power_8023at_power_allocated = TextField(help_text="802.3at power allocated")
    power_8023at_power_requested = TextField(help_text="802.3at power requested")
    med_device_type = TextField(help_text="Chassis MED type")
    med_capability_capabilities = IntegerField(help_text="Is MED capabilities enabled")
    med_capability_policy = IntegerField(help_text="Is MED policy capability enabled")
    med_capability_location = IntegerField(help_text="Is MED location capability enabled")
    med_capability_mdi_pse = IntegerField(help_text="Is MED MDI PSE capability enabled")
    med_capability_mdi_pd = IntegerField(help_text="Is MED MDI PD capability enabled")
    med_capability_inventory = IntegerField(help_text="Is MED inventory capability enabled")
    med_policies = TextField(help_text="Comma delimited list of MED policies")
    vlans = TextField(help_text="Comma delimited list of vlan ids")
    pvid = TextField(help_text="Primary VLAN id")
    ppvids_supported = TextField(help_text="Comma delimited list of supported PPVIDs")
    ppvids_enabled = TextField(help_text="Comma delimited list of enabled PPVIDs")
    pids = TextField(help_text="Comma delimited list of PIDs")

    class Meta:
        table_name = "lldp_neighbors"
